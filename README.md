# Browser-Navigation-System
A Browser Navigation System is designed to simulate the working of a web browser’s Back and Forward navigation features. When a user visits different web pages, the browser keeps track of the pages they have visited. Using this history, the user can move backward to the previous page or forward to the next page.  its follows the LIFO
